{
  "name": "zai-agent-audited-compute",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node tools/dev.js",
    "dev:api": "node apps/api/src/server.js",
    "dev:mock": "node apps/mock-agent/server.js",
    "dev:fancy": "npm --prefix apps/compute-outsourcing-platform run dev",
    "check": "node --check apps/api/src/server.js && node --check apps/mock-agent/server.js && node --check apps/mock-agent/vercelHandler.js && node --check api/health.js && node --check api/v1/intake.js && node --check api/v1/execute.js && node --check api/v1/artifacts/download.js && node --check api/v1/human-market/spec.js && node --check tools/dev.js && node --check scripts/deploy-sepolia.cjs && npm run check:secrets",
    "check:secrets": "node scripts/check-secrets.cjs",
    "contracts:compile": "hardhat compile",
    "contracts:test": "hardhat test",
    "contracts:deploy:sepolia": "hardhat run scripts/deploy-sepolia.cjs --network sepolia"
  },
  "dependencies": {
    "ethers": "^6.16.0",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "@types/jszip": "^3.4.0",
    "hardhat": "^2.22.19"
  }
}
